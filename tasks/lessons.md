# Lessons

- When the user asks for workflow orchestration, switch to an explicit planning flow immediately:
  - Write `tasks/todo.md` before implementation.
  - Treat verification as a first-class part of the plan.
  - Re-plan instead of continuing blindly if scope changes.
- When the user asks for subagent-heavy execution, delegate bounded research and analysis tasks early to keep main-context work focused.
